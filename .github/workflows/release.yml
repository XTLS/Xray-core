 on:
  workflow_dispatch:
   push:
     branches:
       - main
 name: release-please
 jobs:
   release-please:
     runs-on: ubuntu-latest
     steps:
       - uses: GoogleCloudPlatform/release-please-action@v2
         with:
           token: ${{ secrets.TOKEN }}
           release-type: node
           package-name: test-release-please-action
